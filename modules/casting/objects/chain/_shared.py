"""@module casting.objects.chain._shared — what the chain row classes share (constants, seeds, helpers); split from chain_basis.py (sap-2c)."""

STAGE_KINDS = ('cast', 'conversion')
FILL_METHODS = ('gravity-pour', 'inject', 'press', 'in-place')
