"""pspp — Processing → Structure → Properties → Performance foundation.

The PSPP materials framework (PSPP_MATERIALS_PLAN.md at the suite root):
materials evolve through a DAG of durable states; every predicted value
is a CLAIM carrying its evidence method, assumptions and validity; book
figures/tables live as DigitizedDataset rows read by one generic
interpolation engine (never re-coded per figure).

pspp-1 (this slice): the evidence + claims + dataset foundation —
EvidenceMethod vocabulary (unifying materialsScience DERIVATION_METHODS),
Property/Structure/ValidationClaim rows, DigitizedDataset rows seeded
from the geopolymer book Ch.5 transcriptions, and the interpolation
engine with UNSUPPORTED-extrapolation refusals (plan invariant I6).

Later phases add MaterialState DAG (pspp-2), structure (pspp-3),
processes (pspp-4), transfers (pspp-5) — see the plan.
"""
