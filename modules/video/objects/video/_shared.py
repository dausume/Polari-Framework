"""@module video.objects.video._shared — what the video row classes share (constants, seeds, helpers); split from video_basis.py (sap-2c)."""

STATUS_VALUES = ('pending', 'converting', 'ready', 'failed')
