"""@module scoring.objects.score_group._shared — what the score_group row classes share (constants, seeds, helpers); split from score_group_basis.py (sap-2c)."""

GROUP_TYPES = ('political', 'professional', 'civic', 'custom')
