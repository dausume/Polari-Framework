pip==25.3
cffi==2.0.0
cryptography==46.0.4
falcon==4.2.0
# PyJWT (not the `jwt` package) — needed for PyJWKClient + RS256 JWKS-based
# verification of Keycloak access tokens. The [crypto] extra pulls in the
# cryptography backend PyJWT uses for RS256.
PyJWT[crypto]==2.10.1
psutil==5.9.8
pycparser==2.21
PyYAML==6.0.2
requests==2.32.5
setuptools==70.0.0
svgwrite==1.4.3
tippecanoe
minio==7.2.15
urllib3==2.6.3
websockets>=13.0,<14.0
# Equation / calculus support — see polariNoCode/equation_executor.py
sympy==1.13.3
# Parallel execution backend for pure solution-search attempts — see
# simulations/execution_backend.py + DISTRIBUTED_COMPUTE_PLAN.md.
dask[distributed]==2024.12.1
antlr4-python3-runtime==4.11
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
# DB layer switchability (MATERIALS_SCIENCE_MODULE_PLAN.md Track 3):
# MariaDB object-DB adapter + KeyDB (redis-protocol) cache tier.
PyMySQL==1.1.1
redis==5.2.1
# FEM + DFT engine layer (materialsScience/engines/): scikit-fem is the
# pure-Python OO FEM library (numpy/scipy only); ASE is the OO calculator
# layer over DFT codes — Quantum ESPRESSO per Dustin's research notes
# (the pw.x binary itself is NOT baked into the image; the engine
# reports capability honestly and suggests the knob).
scikit-fem==10.0.2
ase==3.24.0
