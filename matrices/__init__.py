# Matrices subsystem — MatrixDefinition persistence + a pure evaluator that
# resolves a matrix (including equation-typed elements and matrix-of-matrices
# composition) down to a concrete NumPy array.
